# PPM_debugging_tools.py
Takes a text file in PPM format where each line start with a space and then 3 decimal values followed by spaces, representing the RGB value of a pixel. This file then produces output files of many helpful formats.
